curl https://start.spring.io/starter.tgz -d dependencies=web,data-mongodb -d language=java -d groupId=com.jberdeja -d artifactId=non-relational-database-administrator -d type=maven-project | tar -xzvf -  

docker compose up  
